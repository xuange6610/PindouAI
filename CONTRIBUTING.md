# 参与贡献

1. Fork 仓库并从 `main` 创建功能分支。
2. 保持改动聚焦，不提交 `.env`、密钥、构建缓存、APK/EXE 或个人数据。
3. Flutter 改动至少运行 `flutter analyze` 和相关 `flutter test`；共享逻辑或发布前运行完整测试。
4. UI 改动同时检查桌面和手机尺寸，保证无文字溢出、遮挡和不可点击控件。
5. 提交 Pull Request，说明动机、实际改动、验证命令和仍未验证的范围。

提交即表示你有权按 Apache-2.0 提供相关代码。第三方图片、字体、商标或数据必须同时说明来源与授权。
