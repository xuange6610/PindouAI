# 发布文件与校验

当前公开版本为 `2.8.10+28`，Git 标签为 `v2.8.10`。所有安装文件从 [GitHub Release](https://github.com/xuange6610/PindouAI/releases/tag/v2.8.10) 下载，不放入源码 Git 历史。

## 正式附件

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `PindouAI-Android-v2.8.10.apk` | 1,852,245,145 | `AB4EB09C163D1762F580E38F4B397DD4E0B55FA3303EE8E60D4EF795E94CB2F6` |
| `PindouAI-Windows-v2.8.10-x64-Setup.exe` | 1,245,157,779 | `A5A7F1D1413828A57ADBD7154D15794ABCA0624B098B66A8EC11302DECF569B5` |
| `PindouAI-Android-v2.8.10-Lite.apk` | 24,386,620 | `C6215F401C6F465A353835300362A83C2E421B7002F22D5BB9396004324E27BB` |
| `PindouAI-Windows-v2.8.10-x64-Lite-Setup.exe` | 20,944,869 | `D68814AB3085A63E71D9EBA60188BACCC7834513BAADBF78188D8EED845E02D9` |
| `SHA256SUMS.txt` | 414 | 校验上述四个安装包 |

完整包包含 1265 张授权图纸；Lite 包不含授权图纸原图和预览，仅保留 4 个开源示例。APK 使用项目测试签名，适合安装体验，不用于应用商店上架。

## Windows 校验

```powershell
Get-FileHash .\PindouAI-Android-v2.8.10.apk -Algorithm SHA256
Get-FileHash .\PindouAI-Windows-v2.8.10-x64-Setup.exe -Algorithm SHA256
Get-FileHash .\PindouAI-Android-v2.8.10-Lite.apk -Algorithm SHA256
Get-FileHash .\PindouAI-Windows-v2.8.10-x64-Lite-Setup.exe -Algorithm SHA256
```

## Linux/macOS 校验

```bash
sha256sum PindouAI-Android-v2.8.10.apk
sha256sum PindouAI-Windows-v2.8.10-x64-Setup.exe
sha256sum PindouAI-Android-v2.8.10-Lite.apk
sha256sum PindouAI-Windows-v2.8.10-x64-Lite-Setup.exe
```

输出应与 Release 和 `SHA256SUMS.txt` 完全一致。不同则删除文件并从官方 Release 重新下载。

## 已执行的发布验证

- `flutter analyze` 通过，无分析问题。
- `flutter test` 126 项通过。
- APK zipalign 和 APK Signature Scheme v2 验证通过。
- APK 内置原图条目 1265/1265。
- Windows 完整安装器包含 1265 张图纸；Windows Lite 静默安装成功，桌面和开始菜单入口存在且无 `artwork` 目录。
- Windows 程序可启动且窗口正常响应。

GitHub 自动生成的 `Source code (zip)` 和 `Source code (tar.gz)` 是源码快照，不是完整安装包，也不替代 Git LFS 的完整资源拉取。

下一步：[版权、安全与参与贡献](17-版权安全与参与贡献)。
